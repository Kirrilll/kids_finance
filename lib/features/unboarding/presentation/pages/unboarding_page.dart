import 'package:flutter/material.dart';

class UnboardingPage extends StatelessWidget {
  const UnboardingPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(body: Center(child: Text('UNBOARDING')));
  }
}
